import { Users } from 'lucide-react';
import { ComingSoon } from '@/components/ComingSoon';

export default function StatistiqueClientPage() {
  return (
    <ComingSoon
      icon={Users}
      title="Statistiques par client"
      description="Analyse des volumes et performances par client : bientôt disponible."
    />
  );
}
